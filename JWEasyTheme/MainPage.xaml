<Page
    x:Class="JWEasyTheme.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:JWEasyTheme"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Loaded="Page_Loaded"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition
                Height="*" />
        </Grid.RowDefinitions>
        <ListView
            x:Name="LV"
            Header="Theme Brush"
            IsItemClickEnabled="False"
            SelectionMode="None">
            <ListView.ItemContainerStyle>
                <Style
                    TargetType="ListViewItem">
                    <Setter
                        Property="HorizontalAlignment"
                        Value="Stretch" />
                    <Setter
                        Property="HorizontalContentAlignment"
                        Value="Stretch" />
                    <Setter
                        Property="Padding"
                        Value="0" />
                    <Setter
                        Property="MinHeight"
                        Value="20" />
                </Style>
            </ListView.ItemContainerStyle>
            <ListView.ItemTemplate>
                <DataTemplate
                    x:Name="TwoLineDataTemplate"
                    x:DataType="local:LVData">
                    <Grid
                        MinHeight="20"
                        Padding="8,4,8,4"
                        Margin="8,4,8,4"
                        BorderBrush="Gray"
                        BorderThickness="1"
                        CornerRadius="5"
                        Background="Transparent">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition />
                            <ColumnDefinition />
                            <ColumnDefinition />
                            <ColumnDefinition />
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition
                                Height="auto" />
                            <RowDefinition
                                Height="75" />
                        </Grid.RowDefinitions>
                        <TextBlock
                            Grid.Row="0"
                            Grid.RowSpan="2"
                            Text="{x:Bind Bname,Mode=OneWay}" />
                        <Grid
                            RequestedTheme="Dark"
                            Margin="8"
                            Grid.Column="1"
                            Background="{ThemeResource SystemAltHighColor}">
                            <TextBlock
                                Text="This is a Test" />
                        </Grid>
                        <Grid
                            RequestedTheme="Light"
                            Margin="8"
                            Grid.Column="2"
                            Background="{ThemeResource SystemAltHighColor}">
                            <TextBlock
                                Text="This is a Test" />
                        </Grid>
                        <Grid
                            RequestedTheme="Default"
                            Margin="8"
                            Grid.Column="3"
                            Background="{ThemeResource SystemAltHighColor}">
                            <TextBlock
                                Text="This is a Test" />
                        </Grid>
                        <Grid
                            RequestedTheme="Dark"
                            Margin="8"
                            Grid.Row="1"
                            Grid.Column="1"
                            HorizontalAlignment="Stretch"
                            Background="BurlyWood" />
                        <Grid
                            RequestedTheme="Light"
                            Margin="8"
                            Grid.Row="2"
                            Grid.Column="2"
                           
                            HorizontalAlignment="Stretch"
                            Background="BurlyWood" />
                        <Grid
                            RequestedTheme="Default"
                            Margin="8"
                            Grid.Row="2"
                            Grid.Column="3"
                            
                            HorizontalAlignment="Stretch"
                            Background="BurlyWood" />
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
